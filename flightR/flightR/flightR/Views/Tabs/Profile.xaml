<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:constants="clr-namespace:flightR;assembly=flightR"
             x:Class="flightR.Views.Tabs.Profile"
             xmlns:local="clr-namespace:flightR;assembly=flightR"
             Title="Profile">

    <!--<ListView x:Name="recordList">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Latitude}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>-->

    <ContentPage.ToolbarItems>
        <ToolbarItem Name="Yenile" Order="Secondary" Activated="onRefresh" Priority="0"></ToolbarItem>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Gelmedi daha" x:Name="lbl"></Label>
            <ListView x:Name="recordList" IsPullToRefreshEnabled="True" Refreshing="onRefresh">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Latitude}" Detail="{Binding Altitude}"></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>